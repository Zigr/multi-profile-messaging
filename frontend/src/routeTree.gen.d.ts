// src/routeTree.gen.d.ts (temporary shim)
// Your IDE may show the error until Vite generates the file. 
// Run the dev server once. If you need a temporary silence:

declare module '@/routeTree.gen' {
    export const routeTree: any
}